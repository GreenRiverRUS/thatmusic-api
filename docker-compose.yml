version: '2'

volumes:
  auth_data:
  audio_cache:

services:
  backend:
    build: src
    image: thatmusic_backend
    restart: always
    env_file:
      - .env
    volumes:
      - auth_data:/auth
      - audio_cache:/cache
    ports:
      - ${CONTAINER_PORT}:8000
